---
summary: "Pre-launch polish: code simplification in fred-get-observations and fred-get-release, AGENTS.md added, Dockerfile OCI labels and .env.example filled in, bunfig.toml added."
breaking: false
---

# 0.1.5 — 2026-05-23

## Changed

- **`fred-get-observations.tool.ts`** — extracted `truncateInline()` helper to de-duplicate the inline-cap loop (called from two code paths); condensed the `fredBody` extraction from a let-block into a single expression.
- **`fred-get-release.tool.ts`** — collapsed multi-step exact-match lookup into a single expression; simplified `hasSearch` guard from `&& trim().length > 0` to optional-chain form; removed a redundant inline comment.
- **`AGENTS.md`** — added as a copy of CLAUDE.md for non-Claude agent runtimes.
- **`Dockerfile`** — filled in `org.opencontainers.image.description` and added `org.opencontainers.image.source` OCI labels.
- **`.env.example`** — added Canvas (`CANVAS_PROVIDER_TYPE`) and all server-specific env vars with defaults.
- **`bunfig.toml`** — added Bun runtime config (`install.auto = fallback`, `run.bun = true`).
