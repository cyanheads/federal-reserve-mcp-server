---
summary: "Maintenance: mcp-ts-core ^0.9.5, explicit zod dep, MCPB bundle support, async cleanup."
breaking: false
security: false
---

# 0.1.2 — 2026-05-23

## Added

- **MCPB bundle support** — `manifest.json`, `.mcpbignore`, and `bundle`/`lint:packaging`/`audit:refresh`/`publish-mcp` scripts for one-click Claude Desktop install via `bun run bundle`.

## Changed

- **`@cyanheads/mcp-ts-core`** `^0.9.1 → ^0.9.5`.
- **`zod`** `^4.4.3` added as an explicit dependency (was a transitive-only dep).
- **`canvas-bridge.ts`** — replaced `as unknown as Parameters<...>[1]` cast with `as RequestContextLike` (type fix from mcp-ts-core 0.9.3).
- **`fred-service.ts`** — removed spurious `async` from 11 methods that return promises without `await`.
- **`fred-get-release.tool.ts`** — removed stale `biome-ignore` suppression.
- Skills synced: `field-test` 2.4→2.5, `maintenance` 2.1→2.4, `polish-docs-meta` 1.8→2.1, `release-and-publish` 2.2→2.4.
- Scripts synced: `devcheck.ts` updated, `lint-packaging.ts` added.
