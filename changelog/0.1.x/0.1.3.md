---
summary: "Error code semantics, mcp-ts-core ^0.9.5 → ^0.9.6, LICENSE added."
breaking: false
security: false
---

# 0.1.3 — 2026-05-23

## Added

- **`LICENSE`** — Apache-2.0 license file added to the repository.

## Changed

- **`@cyanheads/mcp-ts-core`** `^0.9.5 → ^0.9.6`.
- Skills synced: `polish-docs-meta` 2.1→2.2, `release-and-publish` 2.4→2.5.
- **`scripts/lint-packaging.ts`** — manifest name-scope validation and `user_config` field alignment checks added.

## Fixed

- **`fred-get-observations.tool.ts`** — `frequency_too_high` error used `InvalidParams` instead of `ValidationError`; corrected.
- **`fred-get-release.tool.ts`** — `ambiguous_release_search` error used `InvalidParams` instead of `ValidationError`; corrected. Raw `throw new Error` calls replaced with `validationError`/`notFound` factories.
- **`fred-browse-categories.tool.ts`** — raw `throw new Error` calls replaced with typed error factories.
